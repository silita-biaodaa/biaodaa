<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.silita.biaodaa.dao.BorrowMapper">
    <select id="insertBorrow" parameterType="Map">
        insert into mishu_write.borrowing(projName, projNameId, borrower, borrowTime, kbTime, phone, createDate, region, money)
         values(#{projName}, #{projNameId}, #{borrower}, #{borrowTime}, #{kbTime}, #{phone}, NOW(), #{region}, #{money})
    </select>
</mapper>